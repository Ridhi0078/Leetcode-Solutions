class Solution {
    public int minOperations(String s) {
        int max = 0;

        for (char ch : s.toCharArray()) {
            if (ch == 'a') {
                continue;
            }

            max = Math.max(max, 'z' - ch + 1);
        }
        return max;
    }
}
