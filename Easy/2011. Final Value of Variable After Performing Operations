class Solution {
    public int finalValueAfterOperations(String[] operations) {
        int count=0;
        for(char i=0;i<operations.length;i++){
            if((operations[i].equals("--X")) || (operations[i].equals("X--"))){
                count--;
            }else {
                count++;
            }
        }
        return count;
    }
}
