class Solution {
    public int singleNumber(int[] nums) {
        int ans = 0;
        for (int i = 0; i < nums.length; i++) {
            ans ^= nums[i]; //xor operation of same numbers is 0 and xor of any number with 0 is that number only
        }
        return ans;
    }
}
